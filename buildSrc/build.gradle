apply plugin: 'groovy'

buildscript {
	repositories{
	}
	dependencies {
	}
}

repositories {
	maven{ url "https://plugins.gradle.org/m2/" } // for gradle-git
}

dependencies {
	compile gradleApi()
	compile localGroovy()
	compile "org.ajoberstar:gradle-git:1.3.0" // for git interactions
}
